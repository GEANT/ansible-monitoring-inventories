---
# file: host_vars/logs.example.org.yml

# Machine Variables

fqdn: "logs.example.org"

common:
 volumes:
  - volume_dev: "/dev/vdc"
    volume_path: "/data"
    volume_fs: "ext4"
 # Add(present) or Remove(absent) SWAP file. Remove entirely "swap" section to don't use it
 swap:
  name_swapfile: "swapfile"
  size_swapfile: "2048"
  state: present
